<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sunny Burger Juices | Hawassa, Ethiopia</title>
    <meta name="description" content="Flame-Grilled Burgers. Sun-Fresh Juices. Hawassa's favourite spot for bold flavours and fresh goodness.">
    <meta property="og:title" content="Sunny Burger Juices | Hawassa">
    <meta property="og:description" content="Flame-Grilled Burgers. Sun-Fresh Juices. Visit us in Hawassa, Ethiopia.">
    <meta property="og:type" content="website">
    
    <!-- Google Fonts -->
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Playfair+Display:wght@700;800;900&family=Poppins:wght@400;500;600&display=swap');
    </style>

    <style>
        /* ==========================================================================
           CSS VARIABLES & DESIGN SYSTEM
           ========================================================================== */
        :root {
            /* Color Palette */
            --primary: #FF6B35;       /* Sunny Orange */
            --primary-hover: #E85A2A; 
            --secondary: #FFD166;     /* Warm Yellow */
            --accent: #2D6A4F;        /* Deep Green */
            --dark-bg: #1A1A2E;       /* Near-black */
            --dark-bg-glass: rgba(26, 26, 46, 0.85);
            --text-dark: #1C1C1C;     /* Off-black */
            --text-light: #F8F4EF;    /* Warm white */
            --card-bg: #FFF8F0;       /* Warm off-white */
            
            /* Typography */
            --font-heading: 'Playfair Display', serif;
            --font-body: 'Poppins', sans-serif;
            --font-accent: 'Dancing Script', cursive;

            /* Shadows & Transitions */
            --shadow-soft: 0 10px 30px rgba(255, 107, 53, 0.08);
            --shadow-hover: 0 15px 40px rgba(255, 107, 53, 0.2);
            --shadow-dark: 0 20px 40px rgba(0, 0, 0, 0.5);
            --transition-smooth: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            
            /* Layout */
            --container-width: 1200px;
            --section-padding: 100px 20px;
        }

        /* ==========================================================================
           RESET & GLOBAL STYLES
           ========================================================================== */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            font-size: 16px;
        }

        body {
            font-family: var(--font-body);
            color: var(--text-dark);
            background-color: var(--text-light);
            line-height: 1.6;
            overflow-x: hidden;
            -webkit-font-smoothing: antialiased;
        }

        /* Typography Base */
        h1, h2, h3, h4, h5, h6 {
            font-family: var(--font-heading);
            font-weight: 800;
            line-height: 1.2;
            color: var(--text-dark);
        }

        .text-accent {
            font-family: var(--font-accent);
            color: var(--primary);
            font-size: 2.5rem;
            display: block;
            margin-bottom: 0.5rem;
        }

        p {
            margin-bottom: 1rem;
        }

        a {
            text-decoration: none;
            color: inherit;
            transition: var(--transition-smooth);
        }

        img {
            max-width: 100%;
            height: auto;
            display: block;
        }

        ul {
            list-style: none;
        }

        /* Utility Classes */
        .container {
            max-width: var(--container-width);
            margin: 0 auto;
            padding: 0 20px;
        }

        .section {
            padding: var(--section-padding);
        }

        .text-center { text-align: center; }
        
        .section-title {
            font-size: 3rem;
            margin-bottom: 1rem;
            position: relative;
            display: inline-block;
        }

        .section-subtitle {
            font-size: 1.1rem;
            color: #555;
            max-width: 600px;
            margin: 0 auto 3rem auto;
        }

        /* Buttons */
        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 12px 28px;
            font-family: var(--font-body);
            font-weight: 600;
            border-radius: 50px;
            cursor: pointer;
            transition: var(--transition-smooth);
            border: none;
            font-size: 1rem;
            gap: 8px;
        }

        .btn-primary {
            background-color: var(--primary);
            color: var(--text-light);
            box-shadow: 0 4px 15px rgba(255, 107, 53, 0.4);
        }

        .btn-primary:hover {
            background-color: var(--primary-hover);
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 8px 25px rgba(255, 107, 53, 0.6);
            color: var(--text-light);
        }

        .btn-secondary {
            background-color: var(--secondary);
            color: var(--text-dark);
            box-shadow: 0 4px 15px rgba(255, 209, 102, 0.4);
        }

        .btn-secondary:hover {
            background-color: #F8C345;
            transform: translateY(-3px) scale(1.02);
            box-shadow: 0 8px 25px rgba(255, 209, 102, 0.6);
        }

        .btn-pulse {
            animation: pulse 2s infinite;
        }

        /* Animations */
        @keyframes pulse {
            0% { box-shadow: 0 0 0 0 rgba(255, 107, 53, 0.7); }
            70% { box-shadow: 0 0 0 15px rgba(255, 107, 53, 0); }
            100% { box-shadow: 0 0 0 0 rgba(255, 107, 53, 0); }
        }

        @keyframes spinSlow {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        @keyframes bounceDown {
            0%, 20%, 50%, 80%, 100% { transform: translateY(0); }
            40% { transform: translateY(-15px); }
            60% { transform: translateY(-7px); }
        }

        /* Scroll Reveal (Intersection Observer) */
        .reveal {
            opacity: 0;
            transform: translateY(40px);
            transition: all 0.8s cubic-bezier(0.4, 0, 0.2, 1);
        }

        .reveal.active {
            opacity: 1;
            transform: translateY(0);
        }

        .delay-100 { transition-delay: 100ms; }
        .delay-200 { transition-delay: 200ms; }
        .delay-300 { transition-delay: 300ms; }

        /* ==========================================================================
           1. NAVIGATION BAR
           ========================================================================== */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            padding: 20px 0;
            z-index: 1000;
            transition: var(--transition-smooth);
            background: transparent;
        }

        .navbar.scrolled {
            background: var(--dark-bg-glass);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            padding: 12px 0;
            box-shadow: 0 4px 30px rgba(0, 0, 0, 0.1);
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .nav-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .nav-logo {
            font-family: var(--font-heading);
            font-size: 1.5rem;
            font-weight: 800;
            color: var(--text-light);
            display: flex;
            align-items: center;
            gap: 8px;
            letter-spacing: 0.5px;
        }

        .nav-logo span {
            color: var(--secondary);
        }

        .nav-links {
            display: flex;
            gap: 30px;
            align-items: center;
        }

        .nav-link {
            color: var(--text-light);
            font-weight: 500;
            font-size: 0.95rem;
            position: relative;
        }

        .nav-link::after {
            content: '';
            position: absolute;
            bottom: -4px;
            left: 0;
            width: 0;
            height: 2px;
            background-color: var(--primary);
            transition: var(--transition-smooth);
        }

        .nav-link:hover::after {
            width: 100%;
        }

        .hamburger {
            display: none;
            background: none;
            border: none;
            cursor: pointer;
            width: 30px;
            height: 20px;
            position: relative;
            z-index: 1001;
        }

        .hamburger span {
            display: block;
            width: 100%;
            height: 2px;
            background-color: var(--text-light);
            position: absolute;
            transition: var(--transition-smooth);
        }

        .hamburger span:nth-child(1) { top: 0; }
        .hamburger span:nth-child(2) { top: 9px; }
        .hamburger span:nth-child(3) { top: 18px; }

        .hamburger.active span:nth-child(1) {
            transform: rotate(45deg);
            top: 9px;
        }
        .hamburger.active span:nth-child(2) {
            opacity: 0;
        }
        .hamburger.active span:nth-child(3) {
            transform: rotate(-45deg);
            top: 9px;
        }

        /* Mobile Menu */
        .mobile-menu {
            position: fixed;
            top: 0;
            right: -100%;
            width: 80%;
            max-width: 400px;
            height: 100vh;
            background: var(--dark-bg);
            z-index: 999;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            gap: 30px;
            transition: 0.5s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: -10px 0 30px rgba(0,0,0,0.5);
        }

        .mobile-menu.active {
            right: 0;
        }

        .mobile-menu .nav-link {
            font-size: 1.5rem;
            font-family: var(--font-heading);
        }

        /* ==========================================================================
           2. HERO SECTION
           ========================================================================== */
        .hero {
            height: 100vh;
            min-height: 600px;
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            color: var(--text-light);
            overflow: hidden;
        }

        .hero-bg {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, rgba(26, 26, 46, 0.4), var(--dark-bg)), 
                        url('https://images.unsplash.com/photo-1550547660-d9450f859349?ixlib=rb-4.0.3&auto=format&fit=crop&w=1920&q=80') center/cover no-repeat;
            z-index: -1;
            transform: scale(1.05);
            animation: zoomBg 20s infinite alternate linear;
        }

        @keyframes zoomBg {
            from { transform: scale(1); }
            to { transform: scale(1.1); }
        }

        .hero-content {
            position: relative;
            z-index: 1;
            max-width: 900px;
            padding: 0 20px;
        }

        .hero-title {
            font-size: 4.5rem;
            margin-bottom: 20px;
            line-height: 1.1;
            text-shadow: 2px 4px 10px rgba(0,0,0,0.3);
        }

        .hero-title span {
            color: var(--secondary);
        }

        .hero-subtitle {
            font-size: 1.25rem;
            font-weight: 400;
            margin-bottom: 40px;
            color: rgba(255,255,255,0.9);
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .hero-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .floating-badge {
            position: absolute;
            right: 10%;
            top: 25%;
            width: 140px;
            height: 140px;
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(5px);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            font-family: var(--font-accent);
            color: var(--secondary);
            font-size: 1.2rem;
            padding: 20px;
            border: 2px dashed var(--secondary);
            animation: spinSlow 15s linear infinite;
            box-shadow: 0 0 20px rgba(255, 209, 102, 0.2);
            z-index: 2;
        }

        .scroll-indicator {
            position: absolute;
            bottom: 40px;
            left: 50%;
            transform: translateX(-50%);
            animation: bounceDown 2s infinite;
            color: var(--text-light);
            cursor: pointer;
            opacity: 0.7;
            transition: var(--transition-smooth);
        }

        .scroll-indicator:hover {
            opacity: 1;
            color: var(--primary);
        }

        /* ==========================================================================
           3. FEATURE HIGHLIGHTS BAR
           ========================================================================== */
        .features {
            background-color: var(--dark-bg);
            color: var(--text-light);
            padding: 60px 0;
            margin-top: -2px; /* Fix line gap */
            position: relative;
        }

        .features::after {
            content: '';
            position: absolute;
            bottom: -20px;
            left: 0;
            width: 100%;
            height: 40px;
            background: var(--text-light);
            clip-path: polygon(0 50%, 100% 0, 100% 100%, 0% 100%);
            z-index: 10;
        }

        .features-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 30px;
        }

        .feature-card {
            background: rgba(255, 255, 255, 0.03);
            border: 1px solid rgba(255, 255, 255, 0.05);
            padding: 30px 20px;
            border-radius: 16px;
            text-align: center;
            transition: var(--transition-smooth);
        }

        .feature-card:hover {
            transform: translateY(-10px);
            background: rgba(255, 255, 255, 0.06);
            border-color: rgba(255, 107, 53, 0.3);
            box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        }

        .feature-icon {
            font-size: 2.5rem;
            margin-bottom: 15px;
            display: inline-block;
        }

        .feature-title {
            font-family: var(--font-body);
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--secondary);
            margin-bottom: 8px;
        }

        .feature-desc {
            font-size: 0.85rem;
            color: rgba(255,255,255,0.6);
            line-height: 1.4;
        }

        /* ==========================================================================
           4. MENU SECTION
           ========================================================================== */
        .menu-section {
            background-color: var(--text-light);
            position: relative;
            z-index: 11;
        }

        /* Tabs */
        .menu-tabs {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-bottom: 50px;
            flex-wrap: wrap;
        }

        .menu-tab {
            padding: 10px 24px;
            border: 2px solid #EAEAEA;
            background: transparent;
            border-radius: 50px;
            font-family: var(--font-body);
            font-weight: 600;
            color: var(--text-dark);
            cursor: pointer;
            transition: var(--transition-smooth);
        }

        .menu-tab:hover {
            border-color: var(--primary);
            color: var(--primary);
        }

        .menu-tab.active {
            background: var(--primary);
            border-color: var(--primary);
            color: var(--text-light);
            box-shadow: 0 4px 15px rgba(255, 107, 53, 0.3);
        }

        /* Grid & Cards */
        .menu-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 30px;
        }

        .menu-card {
            background: var(--card-bg);
            border-radius: 20px;
            padding: 30px 25px;
            text-align: center;
            box-shadow: var(--shadow-soft);
            transition: var(--transition-smooth);
            position: relative;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            height: 100%;
        }

        .menu-card:hover {
            transform: translateY(-8px);
            box-shadow: var(--shadow-hover);
        }

        /* Shimmer Loading State */
        .shimmer-wrapper {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: var(--card-bg);
            z-index: 10;
            border-radius: 20px;
            display: flex;
            flex-direction: column;
            padding: 30px 25px;
            gap: 20px;
        }

        .shimmer-bg {
            background: linear-gradient(90deg, #f0e6da 25%, #fffbf5 50%, #f0e6da 75%);
            background-size: 200% 100%;
            animation: shimmer 1.5s infinite;
            border-radius: 8px;
        }

        @keyframes shimmer {
            0% { background-position: 200% 0; }
            100% { background-position: -200% 0; }
        }

        .shimmer-icon { height: 70px; width: 70px; border-radius: 50%; margin: 0 auto; }
        .shimmer-title { height: 24px; width: 80%; margin: 0 auto; }
        .shimmer-text { height: 14px; width: 100%; margin: 0 auto; }
        .shimmer-text.short { width: 60%; }
        .shimmer-btn { height: 40px; width: 140px; border-radius: 20px; margin: auto auto 0 auto; }

        /* Actual Content */
        .card-content {
            display: flex;
            flex-direction: column;
            height: 100%;
            opacity: 0;
            transition: opacity 0.5s ease;
        }

        .card-content.visible {
            opacity: 1;
        }

        .menu-icon-wrap {
            width: 80px;
            height: 80px;
            background: white;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            margin: 0 auto 20px auto;
            box-shadow: 0 5px 15px rgba(0,0,0,0.05);
            border: 2px solid transparent;
            transition: var(--transition-smooth);
        }

        .menu-card:hover .menu-icon-wrap {
            border-color: var(--secondary);
            transform: scale(1.1) rotate(5deg);
        }

        .menu-item-title {
            font-size: 1.3rem;
            margin-bottom: 10px;
        }

        .menu-item-desc {
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 25px;
            flex-grow: 1;
        }

        .menu-item-footer {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-top: auto;
            padding-top: 15px;
            border-top: 1px dashed rgba(0,0,0,0.1);
        }

        .menu-price {
            font-weight: 700;
            font-size: 1.1rem;
            color: var(--primary);
        }

        .btn-add {
            background: none;
            border: none;
            color: var(--primary);
            font-weight: 600;
            font-family: var(--font-body);
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 5px;
            transition: var(--transition-smooth);
        }

        .btn-add:hover {
            color: var(--accent);
            transform: translateX(5px);
        }

        /* ==========================================================================
           5. ABOUT SECTION
           ========================================================================== */
        .about-section {
            background-color: var(--text-light);
            background-image: radial-gradient(#FF6B3511 1px, transparent 1px);
            background-size: 20px 20px;
        }

        .about-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 60px;
            align-items: center;
        }

        .about-image-wrapper {
            position: relative;
        }

        .about-image {
            border-radius: 20px;
            box-shadow: var(--shadow-dark);
            position: relative;
            z-index: 2;
        }

        .about-frame {
            position: absolute;
            top: 20px;
            left: -20px;
            width: 100%;
            height: 100%;
            border: 3px solid var(--primary);
            border-radius: 20px;
            z-index: 1;
        }

        .stats-container {
            display: flex;
            gap: 30px;
            margin-top: 40px;
        }

        .stat-item {
            text-align: left;
        }

        .stat-num {
            font-family: var(--font-heading);
            font-size: 2.5rem;
            color: var(--primary);
            line-height: 1;
            margin-bottom: 5px;
        }

        .stat-label {
            font-size: 0.85rem;
            font-weight: 600;
            color: #555;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        /* ==========================================================================
           6. SIGNATURE ITEM SPOTLIGHT
           ========================================================================== */
        .signature-section {
            background: linear-gradient(135deg, var(--dark-bg) 0%, #2A1A3E 100%);
            color: var(--text-light);
            padding: 120px 0;
            position: relative;
            clip-path: polygon(0 5%, 100% 0, 100% 95%, 0 100%);
            margin: 40px 0;
        }

        .signature-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 50px;
            align-items: center;
        }

        .signature-content h2 {
            font-size: 3.5rem;
            color: var(--secondary);
            margin-bottom: 20px;
        }

        .signature-content p {
            font-size: 1.1rem;
            color: rgba(255,255,255,0.8);
            margin-bottom: 30px;
            max-width: 500px;
        }

        .signature-price {
            font-size: 2rem;
            font-family: var(--font-heading);
            color: var(--primary);
            margin-bottom: 30px;
            display: block;
        }

        .signature-img {
            border-radius: 50%;
            box-shadow: 0 0 50px rgba(255, 107, 53, 0.3);
            border: 10px solid rgba(255,255,255,0.05);
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        /* ==========================================================================
           7. CUSTOMER REVIEWS
           ========================================================================== */
        .reviews-section {
            background-color: var(--card-bg);
            overflow: hidden;
        }

        .reviews-scroll {
            display: flex;
            gap: 30px;
            overflow-x: auto;
            scroll-snap-type: x mandatory;
            padding: 20px 0 40px 0;
            -ms-overflow-style: none;
            scrollbar-width: none;
        }

        .reviews-scroll::-webkit-scrollbar {
            display: none;
        }

        .review-card {
            min-width: 350px;
            max-width: 400px;
            background: rgba(26, 26, 46, 0.95);
            color: var(--text-light);
            padding: 40px 30px;
            border-radius: 20px;
            scroll-snap-align: center;
            box-shadow: var(--shadow-dark);
            position: relative;
            flex-shrink: 0;
        }

        .review-card::before {
            content: '"';
            position: absolute;
            top: 10px;
            right: 20px;
            font-family: var(--font-heading);
            font-size: 6rem;
            color: rgba(255,255,255,0.05);
            line-height: 1;
        }

        .stars {
            color: var(--secondary);
            font-size: 1.2rem;
            margin-bottom: 20px;
        }

        .review-text {
            font-size: 1.05rem;
            font-style: italic;
            margin-bottom: 30px;
            color: rgba(255,255,255,0.9);
        }

        .reviewer-name {
            font-weight: 600;
            color: var(--primary);
            font-size: 1rem;
        }

        .reviewer-loc {
            font-size: 0.85rem;
            color: rgba(255,255,255,0.5);
        }

        /* ==========================================================================
           8. FIND US SECTION
           ========================================================================== */
        .find-us-grid {
            display: grid;
            grid-template-columns: 1.5fr 1fr;
            gap: 40px;
            background: #fff;
            border-radius: 24px;
            overflow: hidden;
            box-shadow: var(--shadow-soft);
        }

        .map-container iframe {
            width: 100%;
            height: 100%;
            min-height: 400px;
            border: none;
        }

        .contact-card {
            padding: 50px 40px;
            background: var(--card-bg);
            display: flex;
            flex-direction: column;
            justify-content: center;
        }

        .contact-item {
            display: flex;
            align-items: flex-start;
            gap: 15px;
            margin-bottom: 25px;
        }

        .contact-icon {
            color: var(--primary);
            font-size: 1.5rem;
            margin-top: 2px;
        }

        .contact-detail h4 {
            font-family: var(--font-body);
            font-size: 1rem;
            margin-bottom: 5px;
        }

        .contact-detail p, .contact-detail a {
            color: #555;
            font-size: 0.95rem;
        }

        .contact-detail a:hover {
            color: var(--primary);
        }

        .social-icons-contact {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .social-circle {
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: var(--dark-bg);
            color: var(--text-light);
            display: flex;
            align-items: center;
            justify-content: center;
            transition: var(--transition-smooth);
        }

        .social-circle:hover {
            background: var(--primary);
            transform: translateY(-3px);
        }

        /* ==========================================================================
           9. NEWSLETTER
           ========================================================================== */
        .newsletter {
            background: linear-gradient(135deg, var(--secondary) 0%, var(--primary) 100%);
            padding: 80px 20px;
            text-align: center;
            color: var(--dark-bg);
        }

        .newsletter h2 {
            font-size: 2.5rem;
            margin-bottom: 15px;
        }

        .newsletter p {
            font-weight: 500;
            margin-bottom: 30px;
        }

        .newsletter-form {
            display: flex;
            max-width: 500px;
            margin: 0 auto;
            background: #fff;
            border-radius: 50px;
            padding: 5px;
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }

        .newsletter-form input {
            flex-grow: 1;
            border: none;
            padding: 15px 25px;
            border-radius: 50px;
            outline: none;
            font-family: var(--font-body);
            font-size: 1rem;
        }

        /* ==========================================================================
           10. FOOTER
           ========================================================================== */
        .footer {
            background-color: var(--dark-bg);
            color: var(--text-light);
            padding: 80px 20px 30px 20px;
        }

        .footer-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            gap: 40px;
            margin-bottom: 60px;
        }

        .footer-logo {
            font-family: var(--font-heading);
            font-size: 2rem;
            color: var(--text-light);
            margin-bottom: 20px;
            display: block;
        }

        .footer-logo span {
            color: var(--secondary);
        }

        .footer-text {
            color: rgba(255,255,255,0.6);
            max-width: 300px;
            margin-bottom: 20px;
        }

        .footer h4 {
            font-size: 1.2rem;
            margin-bottom: 20px;
            color: var(--secondary);
        }

        .footer-links li {
            margin-bottom: 12px;
        }

        .footer-links a {
            color: rgba(255,255,255,0.7);
        }

        .footer-links a:hover {
            color: var(--primary);
            padding-left: 5px;
        }

        .footer-bottom {
            border-top: 1px solid rgba(255,255,255,0.1);
            padding-top: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            color: rgba(255,255,255,0.5);
            font-size: 0.9rem;
            flex-wrap: wrap;
            gap: 20px;
        }

        /* ==========================================================================
           MEDIA QUERIES
           ========================================================================== */
        @media (max-width: 1024px) {
            .hero-title { font-size: 3.5rem; }
            .features-grid { grid-template-columns: repeat(2, 1fr); }
            .about-grid, .signature-grid, .find-us-grid { grid-template-columns: 1fr; }
            .about-image-wrapper { margin-bottom: 30px; width: 90%; margin: 0 auto; }
            .signature-section { clip-path: polygon(0 2%, 100% 0, 100% 98%, 0 100%); padding: 80px 0; }
            .signature-content { text-align: center; order: 2; }
            .signature-img { max-width: 400px; margin: 0 auto; order: 1; }
            .footer-grid { grid-template-columns: 1fr; text-align: center; }
            .footer-text { margin: 0 auto 20px auto; }
        }

        @media (max-width: 768px) {
            :root { --section-padding: 70px 20px; }
            .nav-links, .nav-btn { display: none; }
            .hamburger { display: block; }
            .hero-title { font-size: 2.8rem; }
            .section-title { font-size: 2.2rem; }
            .menu-tabs { flex-direction: column; align-items: stretch; gap: 10px; }
            .menu-tab { text-align: center; }
            .stats-container { flex-direction: column; gap: 20px; text-align: center; }
            .stat-item { text-align: center; }
            .find-us-grid { border-radius: 12px; }
            .map-container iframe { min-height: 300px; }
            .newsletter-form { flex-direction: column; background: transparent; box-shadow: none; gap: 10px; }
            .newsletter-form input, .newsletter-form button { border-radius: 50px; width: 100%; }
            .newsletter-form input { padding: 15px; text-align: center; }
            .footer-bottom { flex-direction: column; text-align: center; }
        }

        @media (max-width: 375px) {
            .hero-title { font-size: 2.2rem; }
            .hero-buttons { flex-direction: column; width: 100%; }
            .btn { width: 100%; }
            .floating-badge { display: none; }
            .features-grid { grid-template-columns: 1fr; }
            .menu-grid { grid-template-columns: 1fr; }
            .review-card { min-width: 300px; }
        }
    </style>
</head>
<body>

    <!-- ================= 1. STICKY NAVIGATION ================= -->
    <nav class="navbar" id="navbar">
        <div class="container nav-container">
            <a href="#" class="nav-logo">
                ☀️ <span>Sunny</span> Burger Juices
            </a>
            
            <ul class="nav-links">
                <li><a href="#hero" class="nav-link">Home</a></li>
                <li><a href="#menu" class="nav-link">Menu</a></li>
                <li><a href="#about" class="nav-link">About</a></li>
                <li><a href="#reviews" class="nav-link">Reviews</a></li>
                <li><a href="#find-us" class="nav-link">Find Us</a></li>
            </ul>

            <div class="nav-btn">
                <a href="#menu" class="btn btn-primary btn-pulse">Order Now</a>
            </div>

            <!-- Hamburger Icon -->
            <button class="hamburger" id="hamburger" aria-label="Toggle Menu">
                <span></span>
                <span></span>
                <span></span>
            </button>
        </div>
    </nav>

    <!-- Mobile Drawer -->
    <div class="mobile-menu" id="mobile-menu">
        <a href="#hero" class="nav-link mobile-link">Home</a>
        <a href="#menu" class="nav-link mobile-link">Menu</a>
        <a href="#about" class="nav-link mobile-link">About</a>
        <a href="#reviews" class="nav-link mobile-link">Reviews</a>
        <a href="#find-us" class="nav-link mobile-link">Find Us</a>
        <a href="#menu" class="btn btn-primary btn-pulse mt-4">Order Now</a>
    </div>

    <!-- ================= 2. HERO SECTION ================= -->
    <header class="hero" id="hero">
        <div class="hero-bg"></div>
        <div class="floating-badge">⭐ Rated #1<br>in Hawassa</div>
        
        <div class="container hero-content">
            <h1 class="hero-title reveal">Flame-Grilled Burgers.<br><span>Sun-Fresh Juices.</span></h1>
            <p class="hero-subtitle reveal delay-100">Hawassa's favourite spot for bold flavours and fresh goodness.</p>
            
            <div class="hero-buttons reveal delay-200">
                <a href="#menu" class="btn btn-primary">🍔 View Menu</a>
                <a href="#find-us" class="btn btn-secondary">📍 Find Us</a>
            </div>
        </div>

        <a href="#features" class="scroll-indicator" aria-label="Scroll Down">
            <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <polyline points="6 9 12 15 18 9"></polyline>
            </svg>
        </a>
    </header>

    <!-- ================= 3. FEATURE HIGHLIGHTS ================= -->
    <section class="features" id="features">
        <div class="container features-grid">
            <div class="feature-card reveal">
                <div class="feature-icon">🔥</div>
                <h3 class="feature-title">Flame-Grilled Daily</h3>
                <p class="feature-desc">Handcrafted smash patties cooked to juicy perfection over real fire.</p>
            </div>
            <div class="feature-card reveal delay-100">
                <div class="feature-icon">🍊</div>
                <h3 class="feature-title">100% Fresh Juices</h3>
                <p class="feature-desc">Squeezed and blended to order. No syrups, just pure natural goodness.</p>
            </div>
            <div class="feature-card reveal delay-200">
                <div class="feature-icon">🚀</div>
                <h3 class="feature-title">Fast Service</h3>
                <p class="feature-desc">Hot burgers and cold juices served with a smile, exactly when you need them.</p>
            </div>
            <div class="feature-card reveal delay-300">
                <div class="feature-icon">🌿</div>
                <h3 class="feature-title">Local Ingredients</h3>
                <p class="feature-desc">Sourcing the freshest produce directly from local Ethiopian farmers.</p>
            </div>
        </div>
    </section>

    <!-- ================= 4. MENU SECTION ================= -->
    <main class="section menu-section" id="menu">
        <div class="container">
            <div class="text-center reveal">
                <h2 class="section-title">What We're Serving</h2>
                <p class="section-subtitle">A perfect balance of savory indulgence and revitalizing freshness.</p>
            </div>

            <div class="menu-tabs reveal delay-100">
                <button class="menu-tab active" data-filter="all">All</button>
                <button class="menu-tab" data-filter="burgers">Burgers 🍔</button>
                <button class="menu-tab" data-filter="juices">Juices 🥤</button>
                <button class="menu-tab" data-filter="sides">Sides 🍟</button>
            </div>

            <div class="menu-grid" id="menu-grid">
                <!-- Javascript will inject cards here -->
            </div>
        </div>
    </main>

    <!-- ================= 5. ABOUT SECTION ================= -->
    <section class="section about-section" id="about">
        <div class="container about-grid">
            <div class="about-image-wrapper reveal">
                <div class="about-frame"></div>
                <img src="https://images.unsplash.com/photo-1586190848861-99aa4a171e90?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80" alt="Chef preparing burger" class="about-image">
            </div>
            
            <div class="about-content reveal delay-100">
                <span class="text-accent">Our Story</span>
                <h2 class="section-title" style="font-size: 2.5rem;">Born in the Heart of Hawassa</h2>
                <p>Sunny Burger Juices was founded with a simple dream: to bring bold, flame-grilled burgers and the freshest natural juices to the streets of Hawassa. Every patty is handcrafted, every juice is blended fresh to order.</p>
                <p>We believe great food brings people together. Whether you're craving a decadent, cheesy smash burger or a revitalizing tropical smoothie, we've created a space where both worlds exist in perfect harmony.</p>
                
                <div class="stats-container">
                    <div class="stat-item">
                        <div class="stat-num"><span class="counter-val" data-target="500">0</span>+</div>
                        <div class="stat-label">Happy Customers Daily</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-num"><span class="counter-val" data-target="15">0</span>+</div>
                        <div class="stat-label">Menu Items</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-num"><span class="counter-val" data-target="1">0</span></div>
                        <div class="stat-label">Location (For Now!)</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ================= 6. SIGNATURE ITEM SPOTLIGHT ================= -->
    <section class="signature-section" id="signature">
        <div class="container signature-grid">
            <div class="signature-content reveal">
                <span class="text-accent" style="color: var(--secondary);">House Signature</span>
                <h2>The Sunny Sunrise Juice</h2>
                <p>Experience the vibrant energy of Hawassa in a glass. Our signature blend combines fresh oranges, sweet mango, tangy passion fruit, and a warming kick of fresh ginger. It's the perfect companion to any of our savory burgers.</p>
                <span class="signature-price">ETB ___</span>
                <a href="#menu" class="btn btn-primary">Add to Order →</a>
            </div>
            <div class="signature-img-wrap reveal delay-200">
                <img src="https://images.unsplash.com/photo-1600271886742-f049cd451bba?ixlib=rb-4.0.3&auto=format&fit=crop&w=1000&q=80" alt="Fresh orange mango juice" class="signature-img">
            </div>
        </div>
    </section>

    <!-- ================= 7. CUSTOMER REVIEWS ================= -->
    <section class="section reviews-section" id="reviews">
        <div class="container">
            <div class="text-center reveal">
                <span class="text-accent">Testimonials</span>
                <h2 class="section-title">What Our Customers Say ❤️</h2>
            </div>

            <div class="reviews-scroll reveal delay-100">
                <!-- Review 1 -->
                <div class="review-card">
                    <div class="stars">⭐⭐⭐⭐⭐</div>
                    <p class="review-text">"Best burger in Hawassa, hands down! The smash patty is perfectly juicy and the service is lightning fast."</p>
                    <div class="reviewer-info">
                        <div class="reviewer-name">Biruk T.</div>
                        <div class="reviewer-loc">Hawassa</div>
                    </div>
                </div>
                <!-- Review 2 -->
                <div class="review-card">
                    <div class="stars">⭐⭐⭐⭐⭐</div>
                    <p class="review-text">"I come here every weekend for the Sunny Sunrise juice. Nothing beats it! Fresh, cold, and absolutely delicious."</p>
                    <div class="reviewer-info">
                        <div class="reviewer-name">Sara M.</div>
                        <div class="reviewer-loc">Hawassa</div>
                    </div>
                </div>
                <!-- Review 3 -->
                <div class="review-card">
                    <div class="stars">⭐⭐⭐⭐⭐</div>
                    <p class="review-text">"Finally a place with amazing burgers AND healthy juices in one spot. My whole family loves it!"</p>
                    <div class="reviewer-info">
                        <div class="reviewer-name">Dawit K.</div>
                        <div class="reviewer-loc">Hawassa</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- ================= 8. FIND US SECTION ================= -->
    <section class="section find-us-section" id="find-us">
        <div class="container text-center reveal">
            <h2 class="section-title">Come Visit Us 📍</h2>
            <p class="section-subtitle">We're right in the neighborhood. Drop by for a bite or a sip!</p>
        </div>
        
        <div class="container find-us-grid reveal delay-100">
            <div class="map-container">
                <iframe src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d3918.3!2d38.484449!3d7.0511207!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x17b145775f3cba7f%3A0x6f4528353eda8038!2sSunny%20burger%20juices!5e0!3m2!1sen!2set!4v1700000000000" allowfullscreen="" loading="lazy" referrerpolicy="no-referrer-when-downgrade"></iframe>
            </div>
            
            <div class="contact-card">
                <h3 style="font-size: 1.8rem; margin-bottom: 30px;">Get in Touch</h3>
                
                <div class="contact-item">
                    <div class="contact-icon">📍</div>
                    <div class="contact-detail">
                        <h4>Location</h4>
                        <p>Hawassa, Ethiopia</p>
                    </div>
                </div>
                
                <div class="contact-item">
                    <div class="contact-icon">📞</div>
                    <div class="contact-detail">
                        <h4>Phone</h4>
                        <a href="tel:+251911234567">+251 911 234 567</a>
                    </div>
                </div>
                
                <div class="contact-item">
                    <div class="contact-icon">🕐</div>
                    <div class="contact-detail">
                        <h4>Hours</h4>
                        <p>Daily: 9:00 AM – 10:00 PM</p>
                    </div>
                </div>
                
                <div class="contact-item">
                    <div class="contact-icon">📧</div>
                    <div class="contact-detail">
                        <h4>Email</h4>
                        <a href="mailto:info@sunnyburger.et">info@sunnyburger.et</a>
                    </div>
                </div>

                <div class="social-icons-contact">
                    <a href="#" class="social-circle" aria-label="Instagram">IG</a>
                    <a href="#" class="social-circle" aria-label="TikTok">TK</a>
                    <a href="#" class="social-circle" aria-label="Facebook">FB</a>
                    <a href="#" class="social-circle" aria-label="Telegram">TG</a>
                </div>
            </div>
        </div>
    </section>

    <!-- ================= 9. NEWSLETTER ================= -->
    <section class="newsletter reveal">
        <div class="container">
            <h2>🌟 Get Exclusive Deals & New Menu Alerts!</h2>
            <p>Join the Sunny family and be the first to know about new flavors.</p>
            <form class="newsletter-form" onsubmit="event.preventDefault(); alert('Thanks for subscribing!');">
                <input type="email" placeholder="Enter your email address" required>
                <button type="submit" class="btn btn-dark" style="background: var(--dark-bg); color: #fff; border-radius: 50px;">Subscribe</button>
            </form>
        </div>
    </section>

    <!-- ================= 10. FOOTER ================= -->
    <footer class="footer">
        <div class="container">
            <div class="footer-grid">
                <div class="footer-col">
                    <a href="#" class="footer-logo">☀️ <span>Sunny</span> Burger Juices</a>
                    <p class="footer-text">Flame-Grilled Burgers. Sun-Fresh Juices. Bringing the best flavors to Hawassa every single day.</p>
                </div>
                <div class="footer-col">
                    <h4>Quick Links</h4>
                    <ul class="footer-links">
                        <li><a href="#hero">Home</a></li>
                        <li><a href="#menu">Our Menu</a></li>
                        <li><a href="#about">About Us</a></li>
                        <li><a href="#find-us">Contact</a></li>
                    </ul>
                </div>
                <div class="footer-col">
                    <h4>Follow Us</h4>
                    <ul class="footer-links">
                        <li><a href="#">Instagram</a></li>
                        <li><a href="#">TikTok</a></li>
                        <li><a href="#">Facebook</a></li>
                        <li><a href="#">Telegram</a></li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom">
                <p>© 2026 Sunny Burger Juices — Hawassa, Ethiopia 🇪🇹</p>
                <p>Made with ❤️ in Hawassa</p>
            </div>
        </div>
    </footer>

    <!-- ================= JAVASCRIPT ================= -->
    <script>
        document.addEventListener('DOMContentLoaded', () => {
            
            // --- 1. Menu Data & Rendering ---
            const menuData =;

            const menuGrid = document.getElementById('menu-grid');

            // Generate HTML for Cards with Shimmer
            function renderMenu(items) {
                menuGrid.innerHTML = '';
                items.forEach(item => {
                    const cardHTML = `
                        <div class="menu-card reveal active" data-category="${item.cat}">
                            <!-- Shimmer Loading State -->
                            <div class="shimmer-wrapper">
                                <div class="shimmer-bg shimmer-icon"></div>
                                <div class="shimmer-bg shimmer-title"></div>
                                <div class="shimmer-bg shimmer-text"></div>
                                <div class="shimmer-bg shimmer-text short"></div>
                                <div class="shimmer-bg shimmer-btn"></div>
                            </div>
                            
                            <!-- Actual Content -->
                            <div class="card-content">
                                <div class="menu-icon-wrap">${item.icon}</div>
                                <h3 class="menu-item-title">${item.name}</h3>
                                <p class="menu-item-desc">${item.desc}</p>
                                <div class="menu-item-footer">
                                    <span class="menu-price">ETB ___</span>
                                    <button class="btn-add">Add to Order →</button>
                                </div>
                            </div>
                        </div>
                    `;
                    menuGrid.insertAdjacentHTML('beforeend', cardHTML);
                });

                // Simulate loading time to show shimmer
                setTimeout(() => {
                    document.querySelectorAll('.shimmer-wrapper').forEach(el => el.style.display = 'none');
                    document.querySelectorAll('.card-content').forEach(el => el.classList.add('visible'));
                }, 800);
            }

            // Initial render
            renderMenu(menuData);

            // Menu Tab Filtering
            const tabs = document.querySelectorAll('.menu-tab');
            tabs.forEach(tab => {
                tab.addEventListener('click', (e) => {
                    // Update active tab
                    tabs.forEach(t => t.classList.remove('active'));
                    e.target.classList.add('active');

                    // Filter logic
                    const filter = e.target.getAttribute('data-filter');
                    const filteredItems = filter === 'all' ? menuData : menuData.filter(item => item.cat === filter);
                    
                    // Re-render with animation reset
                    renderMenu(filteredItems);
                });
            });

            // --- 2. Navbar Scroll Effect ---
            const navbar = document.getElementById('navbar');
            window.addEventListener('scroll', () => {
                if (window.scrollY > 50) {
                    navbar.classList.add('scrolled');
                } else {
                    navbar.classList.remove('scrolled');
                }
            });

            // --- 3. Mobile Hamburger Menu ---
            const hamburger = document.getElementById('hamburger');
            const mobileMenu = document.getElementById('mobile-menu');
            const mobileLinks = document.querySelectorAll('.mobile-link');

            function toggleMenu() {
                hamburger.classList.toggle('active');
                mobileMenu.classList.toggle('active');
            }

            hamburger.addEventListener('click', toggleMenu);
            mobileLinks.forEach(link => {
                link.addEventListener('click', toggleMenu);
            });

            // --- 4. Intersection Observer for Scroll Reveals ---
            const revealOptions = {
                root: null,
                rootMargin: "0px",
                threshold: 0.15
            };

            const revealObserver = new IntersectionObserver((entries, observer) => {
                entries.forEach(entry => {
                    if (entry.isIntersecting) {
                        entry.target.classList.add('active');
                        observer.unobserve(entry.target);
                    }
                });
            }, revealOptions);

            document.querySelectorAll('.reveal').forEach(el => {
                revealObserver.observe(el);
            });

            // --- 5. Animated Counters ---
            const counterSection = document.getElementById('about');
            const counters = document.querySelectorAll('.counter-val');
            let hasCounted = false;

            const counterObserver = new IntersectionObserver((entries) => {
                if (entries.isIntersecting && !hasCounted) {
                    hasCounted = true;
                    counters.forEach(counter => {
                        const target = +counter.getAttribute('data-target');
                        let count = 0;
                        const duration = 2000; // ms
                        const increment = target / (duration / 16); // 60fps approx

                        const updateCount = () => {
                            count += increment;
                            if (count < target) {
                                counter.innerText = Math.ceil(count);
                                requestAnimationFrame(updateCount);
                            } else {
                                counter.innerText = target;
                            }
                        };
                        updateCount();
                    });
                }
            }, { threshold: 0.5 });

            counterObserver.observe(counterSection);
        });
    </script>
</body>
</html>